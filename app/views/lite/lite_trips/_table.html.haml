%table.table.table-striped#lite_trips_table
  %thead
    %tr
      %th
        %input#lite_trips_master_checkbox{type: "checkbox"}
      %th Date
      %th One-Way Trips
      %th Seniors
      %th Disabled
      %th Vehicle
      %th Start Odometer
      %th End Odometer
      %th Lift Odometer
      %th Pre-Trip
      %th Actions
  %tbody
    - trips.each do |trip|
      %tr{data: {trip_id: trip.id}}
        %td
          %input.trip-selected{type: "checkbox"}
        %td= format_date trip.trip_date
        %td= trip.num_one_way_trips
        %td= trip.num_senior_trips
        %td= trip.num_disabled_trips
        %td= trip.vehicle ? trip.vehicle.name : translate_helper('no_vehicle')
        %td= trip.start_odometer
        %td= trip.end_odometer
        %td= trip.lift_odometer
        %td= check_box_tag 'pre_trip_inspection', '1', trip.pre_trip_inspection, :disabled => true
        %td
          = link_to edit_lite_lite_trip_path(trip.id), style: 'padding-right: 5px;', title: 'Edit' do
            %i.fas.fa-pencil-alt
          = link_to lite_lite_trip_path(trip), method: :delete, title: 'Delete', data: { confirm: "Are you sure you want to delete this trip?" } do
            %i.fa.fa-trash

:javascript
  $(function() {
    //configure_unassigned_trips_table();
  });